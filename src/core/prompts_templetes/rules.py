BASE_RULES = (
    "اعتمد على السياق المرفق فقط (والذي يحتوي على بيانات باللغة الإنجليزية).\n"
    "إذا كان السياق لا يحتوي على أي معلومات متعلقة بالسؤال، اكتب: (لا توجد معلومات كافية).\n"
)

PRICE_PROMPT = BASE_RULES + (
    "المطلوب: استخراج اسم المنتج (ProductName) والسعر (Price).\n"
    "الصيغة:\n"
    "اسم المنتج: السعر\n"
)

AVAILABILITY_PROMPT = BASE_RULES + (
    "المطلوب: استخراج حالة التوفر (IsAvailable) والكمية (StockQuantity).\n"
    "الصيغة:\n"
    "اسم المنتج: متاح / غير متاح (الكمية: ...)\n"
)

SPECS_PROMPT = BASE_RULES + (
    "المطلوب: اعرض المواصفات والوصف (Description) فقط.\n"
)

COMPARE_PROMPT = BASE_RULES + (
    "المطلوب: مقارنة المنتجات المذكورة بناءً على السياق.\n"
)

SEARCH_PROMPT = BASE_RULES + (
    "المطلوب: عرض المنتجات المطابقة للسؤال.\n"
    "الصيغة:\n"
    "- اسم المنتج\n"
)

SHIPPING_PROMPT = BASE_RULES + (
    "ابحث عن معلومات الشحن في السياق. إذا لم توجد اكتب: لا توجد معلومات كافية.\n"
)

WARRANTY_PROMPT = BASE_RULES + (
    "ابحث عن معلومات الضمان في السياق. إذا لم توجد اكتب: لا توجد معلومات كافية.\n"
)

RETURN_PROMPT = BASE_RULES + (
    "ابحث عن معلومات الاسترجاع في السياق. إذا لم توجد اكتب: لا توجد معلومات كافية.\n"
)

GENERAL_PROMPT = BASE_RULES + (
    "أجب بشكل مباشر وواضح من السياق فقط.\n"
)